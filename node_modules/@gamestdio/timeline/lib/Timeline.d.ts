export declare class Timeline {
    startTime: number;
    lastSnapshotTime: number;
    history: any[];
    maxSnapshots: number;
    constructor(maxSnapshots?: number);
    takeSnapshot(state: any, elapsedTime?: number): void;
    /**
     * Get snapshot taken at `elapsedTime` interval.
     */
    at(elapsedTimeAt: number, interpolate?: boolean): any;
    offset(offset: number, interpolate?: boolean): any;
}
